import revertByWorld from '../../code/string/leetcode-557'

test("revertByWorld:Let's take LeetCode contest", () => {
	expect(revertByWorld("Let's take LeetCode contest")).toBe(
		"s'teL ekat edoCteeL tsetnoc"
	);
});